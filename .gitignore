# Extracted from the standalone zip; reproducible.
design_standalone/

# WSL Zone.Identifier alternate-stream files.
*:Zone.Identifier

# Headless test screenshots are regenerated; the manually-captured
# ones in screenshots/ are kept.
build/headless/

# Plugin source intermediate
*.o
.scratch/
tools/.venv/

.claude/
build/

# Dev scratch notes and one-off audits (not part of the shipped repo)
scratchpad/
__pycache__/
*.pyc

# Extracted album covers used to render screenshots (regenerable via ffmpeg)
docs/screens/art/
